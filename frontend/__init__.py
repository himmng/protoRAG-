"""protoRAG+ frontend static server.

Serves `index.html` and `static/` from the project root — nothing else.
Run with `python3 -m frontend`. No Node/Vite required; the frontend talks
to whichever backend URL is configured in the UI's Settings panel.
"""
