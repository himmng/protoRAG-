services:
  - type: web
    name: protorag-backend
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: PROTORAG_API_TOKEN
        generateValue: true
